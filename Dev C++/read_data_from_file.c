#include<stdio.h>
void main(){
	FILE *f,*cu;int i=0;char ch[50],c;int count=0;
	f=fopen("state.txt","r");
	
	if(f==NULL)
	printf("file cant be open\n");
	else

	while((c=fgetc(f))!=EOF){
		
		while((=fgetc(f))!=EOF){
			printf("
}
	fclose(f);
}

